<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="./css/styles.css" />
		<title>Easy</title>

		<style type="text/css">
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			body {
				display: flex;
				flex-direction: column;
				align-content: space-between;
				justify-content: flex-start;
			}

			.container {
				position: fixed;
				border: solid black 2px;
				border-radius: 5px;
				margin-top: 3rem;
				width: 80%;
				height: 90vh;
				left: 5rem;
			}

			h1 {
				text-align: center;
				margin: 1rem;
				padding: 1rem;
				bottom: 38.75rem;
				z-index: 5;
				left: 49rem;
			}
			h1 span {
				border: solid black 1px;
				border-radius: 10px;
				padding: 1rem 2rem;
				background-color: #fff;
			}

			main {
					display: flex;
				justify-content: center;
				align-items: center;
				margin-bottom: 6rem;
				margin-top: 4rem;
				flex-direction: column;
			}
			table:first-of-type{
				margin-bottom: 4rem;
			}

			table {
				border: solid black 1px;
				text-align: center;
				table-layout: fixed;
				width: 31rem;
			}
			table.noSpacing {
				border-spacing: 0;
				border-collapse: collapse;
			}

			.wordSearchTable {
				text-align: center;
				margin-top: 3rem;
			}

			caption {
				caption-side: bottom;
				padding: 1rem;
			}
			thead {
				border-top: hidden;
				border-left: hidden;
				border-right: hidden;
			}

			th {
				padding-bottom: 1rem;
			}
			.wordSearchMeta {
				column-span: all;
				text-align: center;
			}

			tr {
				height: 5px;
			}
			tr:first-child {
				border-top: solid black 1px;
			}
			td {
				padding: -1rem;
				text-align: center;
				text-transform: capitalize;
				font-size: 1.75rem;
			}

			.wordsTable {
				display: flex;
				justify-content: center;
			}

			.wordsTable table td {
				border: hidden;
			}
		</style>
	</head>

	<body>
		<div class="container">`</div>
		<h1><span><%=title%></span></h1>
		<main>
			<table>
  <%
  for (var i = 0; i < wordSearchData.length; i++) { %>
    <tr>
      <% for (var j = 0; j < wordSearchData[i].length; j++) { %>
        <td><%= wordSearchData[i][j] %></td>
      <% } %>
    </tr>
  <% } %>
</table>


			<table>
				<thead>
					<tr>
						<th colspan="3">Can you finds the words below</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td>word</td>
						<td>word</td>
						<td>word</td>
					</tr>
					<tr>
						<td>word</td>
						<td>word</td>
						<td>word</td>
					</tr>
					<tr>
						<td>word</td>
						<td>word</td>
						<td>word</td>
					</tr>
				</tbody>
			</table>
		</aside>
		<footer><span> A word search created by <%=authorName%></span> from the website wordSearchGen.com </footer>
	</body>
</html>

